{
  "joint_names": ["joint_1", "joint_2", "joint_3", "joint_4"],
  "motor_ids_by_joint": [0, 1, 2, 3],
  "angle_field": "q_output_rad",
  "velocity_field": "dq_output_rad_s",
  "torque_field": "tau_ideal_output_nm",
  "direction": [1, 1, 1, 1],
  "zero_offset_rad": [0.0, 0.0, 0.0, 0.0],
  "zero_calibrated": false,
  "direction_calibrated": false,
  "calibrated": false,
  "notes": [
    "Four-axis model: joint_1 through joint_4 use motor IDs 0 through 3 in order. Verify this mapping on the rebuilt arm.",
    "The previous six-axis encoder capture is invalid after the mechanical change, including the changed joint_4 frame. Recapture all four zero positions and verify directions before motion.",
    "config/calibration_pose.json is an offline geometric reference, not a captured hardware calibration.",
    "The reader exposes SDK-side and gear-ratio-converted values. tau_ideal_output_nm multiplies rotor torque by 6.33 and omits gearbox efficiency."
  ]
}
